#include <stdio.h>
void main()
{
     int i, num, a[100], k;
     printf("enter array limit");
     scanf("%d", &num);
     printf("enter array limit");
     for (i = 0; i < num; i++)
     {
          scanf("%d", &a[i]);
     }
     for (i = 0; i < num; i++)
     {
          if (a[i] % 2 == 0)
          {
               for ( k = 0; k < a[i]; k++)
               {
                    a[k]=a
               }
               
          }
     }
}