#include <stdio.h>
void main()
{
     int num, a[100], i, j, l;
     printf("enter the size of the array:");
     scanf("%d", &num);
     printf("enter the values of the array:");
     for (i = 0; i < num; i++)
     {
          scanf("%d", &a[i]);
     }
     l = a[0];
     for (i = 0; i < num; i++)
     {
          if (a[i] > l)
          {
               for ( i = 0; i < count; i++)
               {
                    /* code */
               }
               
          }

     }
     
}